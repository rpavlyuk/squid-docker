= Sample configuration file for Squid
* `squid.conf`: Sample configration which uses PAM authentication and enables HTTPS proxy (required valid SSL certificate)
* `squid-systemdock-config.yml`: Sample configuration for [SystemDock](https://github.com/rpavlyuk/systemdock.git). Can be easily modified to match `docker-compose` format.
